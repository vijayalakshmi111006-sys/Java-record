public class MaxElement {
    public static void main(String[] args) {
        int[] arr = {2, 8, 4, 9, 3};
        int max = arr[0];

        for (int num : arr) {
            if (num > max) max = num;
        }

        System.out.println("Maximum element: " + max);
    }
}
